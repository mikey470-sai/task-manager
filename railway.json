{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && pip install -r requirements.txt && flask db upgrade && gunicorn app:app",
    "restartPolicyType": "ON_FAILURE"
  }
}